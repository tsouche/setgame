# File name: layouts2.kv 
#:kivy 1.9.0

# <Card>:
    #text: "i=" + str(self.i) + " - j=" + str(self.j) +"\n" + str(self.c) + str(self.s) + str(self.f) + str(self.n) + "\n" + self.code
    # size_hint_x: None
    # width: self.card_width
    # size_hint_y: None
    # height: self.card_height
    # canvas:
        # Color:
            # rgba: 0, 0.4, 0, 1
        # draws the background (green)
        # Rectangle:
            # pos: 0, 0
            # size: self.width, self.height
        # drwas the card (white)
        # Color:
            # rgba: 1, 1, 1, 1
        # Rectangle:
            # pos: self.center_x - 4*self.unit, self.center_y - 7.5*self.unit
            # size: self.width * 0.8, self.height
        # Rectangle:
            # pos: 0, self.height / 15
            # size: self.width * 0.1, self.height * 13 / 15
        # Rectangle:
            # pos: self.width * 0.9, self.height / 15
            # size: self.width * 0.1, self.height * 13 / 15
        # Ellipse:
            # pos: 0, 0
            # size: self.width * 0.1, self.width * 0.1
        # Rectangle:
            # pos: root.center_x - self.card_width/2, root.center_y - self.card_height/2
            # size: self.card_width,self.card_height
            # source: self.filename
    
<PositionOnTable>:

<Table>:
    rows: 3
    cols: 4
    spacing: 10
    padding: 7
    canvas:
        Color:
            rgba: 0, 0.4, 0, 1
        Rectangle:
            pos: self.pos
            size: self.size
